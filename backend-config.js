/*
  Paste the Project URL and publishable (or legacy anon) key from your Supabase
  project's Connect dialog. Never place a service_role or secret key here.
*/
window.LEADERBOARD_CONFIG = {
  supabaseUrl: '',
  supabasePublishableKey: ''
};
